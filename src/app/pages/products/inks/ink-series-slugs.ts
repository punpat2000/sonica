/**
 * Screen printing series slugs only. Used by sitemap and any build-time code
 * that must not import ink-series.model (which uses $localize).
 * Keep in sync with SCREEN_PRINTING_SERIES in ink-series.model.ts.
 */
export const SCREEN_PRINTING_SERIES_SLUGS: string[] = [
  '70g-series',
  'ppc-series',
  'msk-series',
  '1500-series',
  'specialty-screen',
];
